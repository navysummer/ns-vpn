[package]
name = "ns-vpn-signal"
version = "0.1.0"
edition = "2024"
rust-version = "1.91"

[dependencies]
ns-vpn-logging = { workspace = true }
log = { workspace = true }
tokio = { workspace = true, features = ["signal"] }

[lints]
workspace = true
