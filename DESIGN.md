# DESIGN.md

## 气质与意象
- **极简专业工具感**：仿照 macOS 原生设计语言的精密仪器感，界面干净利落，信息密度高但不杂乱
- **意象锚点**：航空仪表盘 / 示波器 / 网络拓扑图，每个数字和状态都有意义
- **深色优先**：默认深色主题，暗色背景衬托彩色流量数据，像夜间的控制台

## 配色方案
### 深色模式
- **背景**：`#0f0f11`（主背景）、`#1a1a1e`（卡片/面板）、`#252529`（悬浮/选中态）
- **文字**：`#e8e8ed`（主文字）、`#98989e`（次文字/说明）
- **强调色**：`#4f8ef7`（链接/蓝色强调）、`#34c759`（绿色/成功/在线）、`#ff9f0a`（橙色/警告）、`#ff453a`（红色/错误）
- **边框**：`#38383a`（分割线/边框）

### 浅色模式
- **背景**：`#f5f5f7`（主背景）、`#ffffff`（卡片/面板）、`#e8e8ed`（选中态）
- **文字**：`#1d1d1f`（主文字）、`#86868b`（次文字）
- **强调色**：同深色，保持品牌一致
- **边框**：`#d2d2d7`

## 字体排版
- **显示字体**：Inter（Google Fonts），数字用 SF Mono 风格
- **字号层级**：
  - 页面标题：24px / 600
  - 数字统计：32px / 700（等宽字体）
  - 卡片标题：14px / 500
  - 正文/列表：13px / 400
  - 辅助文字：11px / 400

## 布局与间距
- **侧边栏**：240px 宽，固定在左侧，带图标+文字导航
- **内容区**：右侧自适应，上下 padding 24px，左右 32px
- **卡片**：圆角 12px，内边距 16px，背景色区分层级
- **间距**：卡片间距 12px，内容组间距 24px

## 组件规范
- **按钮**：圆角 8px，高度 32px，无边框纯色填充
- **开关（Toggle）**：iOS 风格，宽度 40px
- **输入框**：圆角 8px，高度 32px，深色模式背景 `#252529`
- **选择器（Select）**：同输入框风格
- **标签（Tag/Badge）**：圆角 4px，内边距 4px 8px，字号 11px
- **进度条**：圆角 2px，高度 4px

## 动效与交互
- **过渡**：所有交互在 150ms-200ms 内完成，ease-out 曲线
- **悬停**：卡片/列表项 hover 时背景色微变，`background-color` 过渡
- **侧边栏**：选中项左侧有 3px 实色指示条
- **加载**：骨架屏方案，脉冲动画

## 设计禁忌
- 不使用渐变背景/大面积纯色块
- 不使用圆角大于 16px 的卡片
- 不使用阴影，用边框区分层级
- 不使用 emoji 作为图标，统一使用 Lucide 图标
- 避免任何毛玻璃/模糊效果